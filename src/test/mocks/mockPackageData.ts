import { CmdCallbackData } from '../../types';
import { packageName } from '.';

const mockPackageData: CmdCallbackData = {
  packageName,
  packageVersion: '1.0.0',
};

export default mockPackageData;
